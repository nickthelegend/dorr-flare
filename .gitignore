node_modules/
.next/
dist/
build/
*.log
.DS_Store

# secrets — never commit
.env
.env.*
!.env.example
wallets/
*.sk
*.skey

# runtime state
services/operator/data/
tools/scripts/*.log
/tmp/

# Midnight localnet runtime DB (regenerated each run)
vendor/zkperps/midnight-level-db/
apps/web/.claude/

# demo take artifacts (large binaries)
demo-take/
.vercel

# The enclave master seed. Every tenant address derives from it and dorr's is
# registered on-chain — losing it means re-registering. Backed up out of Heroku
# before that app was deleted. Never commit.
phala/.enclave-secrets.json

# Narration audio — regenerate with tools/demo/tts.py, do not commit 12MB of wav
audio/*.wav
